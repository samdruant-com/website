/*const fs = require("fs");
const files = fs.readdirSync("./");

function extractImages(files){
 	const images = files.filter(file => file.includes(".webp") || file.includes(".jpg") || file.includes(".png"));


 	const list = images.map((image, index) => {
 		return {
 			_id: index,
 			src: image,
 			place: "",
 			photographer: ""
 		};
 	});

 	console.log(list);
}

extractImages(files);
*/
